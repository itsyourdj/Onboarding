# Source: build follow-up (2026-07-29) — real SLA response-time measure discovered
# ts UPDATED (2026-07-29): now points to CALLBACK_DATE_TIME, a true TIMESTAMP
# column with real time-of-day precision, instead of CALLBACK_DATE (a DATE
# column only cast to TIMESTAMP with no time component).
kind: metric
name: response_time_trend
measure: callbacks.avg_response_time_minutes
ts: callbacks.CALLBACK_DATE_TIME
granularity: day

dimensions:
  - callbacks.PRODUCT_CLASS
  - callbacks.REGION_DESC
  - callbacks.GBO

description: Average minutes between dispatch and on-site arrival, over time — real SLA response-time measure
